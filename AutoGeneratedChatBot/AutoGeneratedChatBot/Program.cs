using System;

namespace AutoGeneratedChatBot
{
    class Program
    {
        static void Main(string[] args)
        {
            TextGenerator gen = new TextGenerator();
            Console.WriteLine("Type 'start' or 'stop' to (de)activate bot");
            gen.Run();
            while (true)
            {
                if (Console.ReadLine().ToLower() == "stop")
                {
                    gen.Stop();
                }
                else if (Console.ReadLine().ToLower() == "start")
                {
                    gen.Run();
                }
            }
        }
    }
}
